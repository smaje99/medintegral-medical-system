import { type Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Inicio | Medintegral IPS S.A.S',
};

const DashboardPage = () => {
  return <div>Dashboard</div>;
};

export default DashboardPage;
